A Vagrant plugin that configures the virtual machine to use specified proxies.
This is useful for example in case you are behind a corporate proxy server, or
you have a caching proxy (for example polipo).

WWW: https://github.com/tmatilai/vagrant-proxyconf
