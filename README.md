## `sysutils/rubygem-vagrant-proxyconf`

FreeBSD port of "a Vagrant plugin that configures the virtual machine to use
specified proxies".

## Status

* successfully built in `poudriere` system
* the port is being tested
